# Test fixtures

Fixtures must use synthetic identities, domains and document metadata. Never add real CVs, identity documents, access tokens, contact details or provider credentials. Phase 0 tests use short inline synthetic values; later phases should keep larger fixtures in this directory and label their provenance.
