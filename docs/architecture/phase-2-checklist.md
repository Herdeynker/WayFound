# WAYFOUND Phase 2 verification checklist

| Requirement                               | Implementation                                                                                          | Evidence                                                | Status                                        |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | --------------------------------------------- |
| Email/password, verification and recovery | `src/app/api/auth/*`, `src/app/auth/*`, `src/features/auth/auth-forms.tsx`                              | Auth E2E, typecheck, build                              | complete                                      |
| Magic link and Google adapter             | `magic-link` and `oauth` routes; callback exchange; disabled state when `GOOGLE_OAUTH_ENABLED` is false | OAuth-disabled E2E and callback code path               | complete/configuration-gated                  |
| Server authorization and safe redirects   | `src/middleware.ts`, `src/server/auth/guards.ts`, `src/server/security/redirect.ts`                     | unauthenticated dashboard E2E and redirect unit tests   | complete                                      |
| Profile/preferences bootstrap             | Phase 2 `auth.users` trigger and `bootstrapAccount` upsert                                              | hosted migration history and RLS integration test       | complete                                      |
| Versioned consent                         | append-only `user_consents`, `PHASE2_POLICY_VERSION`, consent screen/API                                | consent schema and service tests/build                  | complete                                      |
| Audit events                              | `audit_events` insert policy and `recordAudit`; scalar-only metadata                                    | migration policy, logging redaction, source audit       | complete                                      |
| Session/device sign-out                   | local/global Supabase sign-out route and settings UI                                                    | route implementation and lint/typecheck                 | complete                                      |
| Export request foundation                 | idempotent `data_export_requests` queue and retryable worker hook                                       | hosted table/RLS inspection                             | complete foundation; delivery worker pending  |
| Deletion foundation                       | grace-period queue, explicit confirmation, cancellation RPC, injected worker hook                       | hosted table/RLS inspection                             | complete foundation; retention policy pending |
| Cross-user isolation                      | RLS on every Phase 2 user-owned table, no anonymous grants                                              | hosted RLS integration test                             | complete                                      |
| Mobile-first surfaces                     | auth/consent/settings custom Direction A styling                                                        | Playwright auth checks plus Phase 2 screenshot artifact | complete                                      |
| Scope control                             | no landing page, Phase 3 passport or provider success was added                                         | route/source audit                                      | complete                                      |

The export and deletion queues intentionally stop at a safe, retryable foundation. Legal retention duration, artifact delivery, anonymization and final account deletion remain configuration/worker work before production launch; the UI does not represent those hooks as completed delivery.
